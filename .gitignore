# secrets - a real .env must never be committed
.env
.env.*
!.env.example

# python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/

# node (if the frontend is later built with a bundler)
node_modules/
.next/
dist/

# local data that must not be published
backend/data/raw/
*.log

# os
.DS_Store
Thumbs.db
